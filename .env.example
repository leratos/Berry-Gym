# ==================================
# HomeGym - Environment Configuration
# ==================================
#
# 1. Kopiere diese Datei zu .env
# 2. Ersetze alle Platzhalter mit echten Werten
# 3. .env ist in .gitignore und wird NICHT commited!

# ==================================
# Django Core Settings
# ==================================
DEBUG=False
SECRET_KEY=your-secret-key-here-generate-with-manage-py
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# ==================================
# Database Configuration
# ==================================

# Option 1: SQLite (Development)
# Setze DB_ENGINE auf 'django.db.backends.sqlite3'
# DB_NAME ist dann der Pfad zur db.sqlite3

# Option 2: MariaDB/MySQL (Production)
DB_ENGINE=django.db.backends.mysql
DB_NAME=homegym_db
DB_USER=homegym_user
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=3306

# ==================================
# Static/Media Files (Production)
# ==================================
STATIC_ROOT=/path/to/staticfiles/
MEDIA_ROOT=/path/to/media/

# ==================================
# Security Settings (Production)
# ==================================
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# ==================================
# Monitoring & Error Tracking
# ==================================
# Sentry DSN (Error Tracking - nur Production)
# Hol dir einen kostenlosen Account: https://sentry.io/
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ENVIRONMENT=production  # production, staging, development

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==================================
# AI Coach - LLM Configuration
# ==================================

# Ollama (Local LLM - kostenlos, privat)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# OpenRouter (Cloud LLM - ~0.003€ pro Request)
# WICHTIG: API Key mit secrets_manager.py speichern (empfohlen)!
# python ai_coach/secrets_manager.py set OPENROUTER_API_KEY sk-or-v1-xxx
#
# Fallback: Kann auch hier gesetzt werden (weniger sicher):
# OPENROUTER_API_KEY=sk-or-v1-xxx
OPENROUTER_MODEL=meta-llama/llama-3.1-70b-instruct

# DALL-E 3 Image Generation via OpenRouter
# Nutzt gleichen OPENROUTER_API_KEY wie AI Coach
# Kosten: ~$0.04 pro Bild (Standard Quality 1024x1024)
DALLE3_MODEL=openai/dall-e-3

# Automatisch: Server (DEBUG=False) nutzt immer OpenRouter
# Lokal (DEBUG=True) nutzt Ollama, außer USE_OPENROUTER=True
USE_OPENROUTER=False
USE_OPENROUTER_FALLBACK=True

# ==================================
# Optional: Redis (Caching)
# ==================================
# REDIS_URL=redis://127.0.0.1:6379/1

# ==================================
# E-Mail Configuration (SMTP)
# ==================================
# Für: Passwort-Reset, Beta-Einladungen, Registrierungsbestätigung

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=last-strawberry.com
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_USE_TLS=False
EMAIL_HOST_USER=noreply@last-strawberry.com
EMAIL_HOST_PASSWORD=your_email_password_here
DEFAULT_FROM_EMAIL=HomeGym <noreply@last-strawberry.com>

# Site URL (für E-Mail-Links)
SITE_URL=https://gym.last-strawberry.com

# ==================================
# Web Push Notifications
# ==================================
VAPID_PRIVATE_KEY_FILE=vapid_private.pem
VAPID_PUBLIC_KEY_FILE=vapid_public.pem
VAPID_CLAIMS_EMAIL=mailto:noreply@last-strawberry.com

# ==================================
# Deployment (Production)
# ==================================
# Diese .env Datei muss auf dem Server im Projekt-Root liegen.
# Sie wird via python-dotenv automatisch geladen.
#
# Systemd-Service: siehe deployment/homegym.service
# Nginx-Config:    siehe deployment/nginx-snippet.conf (falls vorhanden)
#
# WICHTIG: Variablen in systemd Environment= sind NICHT äquivalent
# zu .env-Variablen, wenn die Namen abweichen.
# settings.py liest ausschliesslich (GENAU diese Namen!):
#   SECRET_KEY        (nicht DJANGO_SECRET_KEY!)
#   DB_ENGINE, DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, DB_PORT
#   (nicht DATABASE_* Varianten!)
