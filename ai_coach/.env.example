# SSH Tunnel Configuration
SSH_HOST=gym.last-strawberry.com
SSH_PORT=22
SSH_USERNAME=dein_ssh_username
SSH_PASSWORD=dein_ssh_passwort
# Oder besser: SSH Key verwenden (Pfad angeben, dann SSH_PASSWORD weglassen)
# SSH_KEY_PATH=/path/to/your/private_key

# MariaDB Configuration (auf dem Server)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gym_datenbank_name
DB_USER=fit
DB_PASSWORD=dein_db_passwort

# Local SSH Tunnel Binding
LOCAL_BIND_PORT=3307

# Django Settings Module (normalerweise nicht ändern)
DJANGO_SETTINGS_MODULE=config.settings

# User ID für Trainingsplan-Generierung
DEFAULT_USER_ID=1

# Ollama Configuration
OLLAMA_MODEL=llama3.1:8b
OLLAMA_HOST=http://localhost:11434
